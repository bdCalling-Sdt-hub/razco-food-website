import React from "react";
import ContactClient from "./ContactClient";

const page = () => {
  
  return (
    <React.Fragment>
      <ContactClient/>
    </React.Fragment>
  );
};

export default page;
