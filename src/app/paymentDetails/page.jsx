import React from "react";
import PaymentClient from "./PaymentClient";

const page = () => {
  return (
    <React.Fragment>
      <PaymentClient/>
    </React.Fragment>
  );
};

export default page;
